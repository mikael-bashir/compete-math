[pytest]
asyncio_mode = auto
pythonpath = .
# Belt-and-suspenders: no single test in this suite should ever legitimately
# take anywhere near this long. If one hangs (e.g. a fixture's server never
# signals started), fail loudly instead of stalling CI indefinitely.
timeout = 30
